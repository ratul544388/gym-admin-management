import { Loader } from "@/components/loader";
import React from "react";

const Loading = () => {
  return <Loader className="abs-center text-primary md:ml-[calc(120px_-_20px)]" />;
};

export default Loading;
